/**
 * The quick one liner solution.
 *
 * @param {String} num1
 * @param {String} num2
 * @return {String}
 */
const multiply = (num1, num2) => {
  return (Number(num1) * Number(num2)).toString();
};
